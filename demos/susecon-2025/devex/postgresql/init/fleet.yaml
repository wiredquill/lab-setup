defaultNamespace: postgresql
labels:
  provides: postgresql-init
overrideTargets:
  - clusterSelector:
      matchLabels:
        demo: "susecon-devex"
  - clusterSelector:
      matchLabels:
        demo: "postgres"


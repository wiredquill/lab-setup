defaultNamespace: apisix
helm:
  chart: oci://dp.apps.rancher.io/charts/apache-apisix
  releaseName: apisix
  valuesFiles:
    - values.yaml
overrideTargets:
  - clusterSelector:
      matchLabels:
        demo: "susecon-devx-downstream"
dependsOn:
  - selector:
      matchLabels:
        provides: application-collection
